# syscopilot

Local CLI that reviews a system design description using Claude Opus and outputs a structured report.

## Setup (Windows CMD)
```cmd
py -m venv venv
venv\Scripts\activate
pip install -U pip
pip install anthropic typer rich python-dotenv pydantic
